services:
  - type: web
    name: lockedin-communicator
    runtime: docker
    envVars:
      - key: PORT
        value: "10000"
      - key: WS_HOST
        value: 0.0.0.0
      - key: STATIC_DIR
        value: frontend/dist
